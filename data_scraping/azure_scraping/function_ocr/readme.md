Applies on each new/modified PDF or image in an Azure blob container. Will produce OCR data as `ocr.json` for each blob.

Example:

- documents/example/test_data.pdf
- documents/example/test_data.pdf/ocr.json 
