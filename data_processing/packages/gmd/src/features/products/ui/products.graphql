query products($input: ProductsInput) {
    products(input: $input) {
        description
        image {
            url
            fallbackUrl
        }
        price {
            currency
            amount
        }
    }
}
